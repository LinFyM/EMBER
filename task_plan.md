# EMBER task plan

## 当前：幅度信用修正未恢复父能力，冻结检验探索目标是否错位（2026-09-25）

成功信用方向与条件化分解已完成，见findings§147–148；方向改变本身不等于闭环修复。
[96条闭环补验](docs/designs/return_score_update_design.md)已完成并独立核验，见findings§149：
P/RAW/RB为15/13/15，RB保留RAW全部13条并新增2条，但新增只在teacher46，原三条父成功仍丢失。
RB与父净分持平、任务breadth却从6降到5；强保持预测未兑现，不继续此修正的长训练或尺度试验。

当前登记[探索目标对齐交叉](docs/designs/return_objective_alignment_design.md)：
固定父P与已保存RB候选，在原八采集teacher×states0..3交叉J0/J_Sigma，共128条；
探索只用一个未进入旧梯度的新replica4，无参数更新、新梯度、额外预测或held输入。
16个新bank、16full、全部T+1 trace，最多1.5 GPU-hours/3GiB；实际派发与接手见progress。
已有梯度针对J_Sigma，而旧评测同时换成独立state/video及J0；追加旧J0面板不能拆开该混杂。
本批直接检验Delta_Sigma、Delta0与二者交互，区分探索目标未迁移与原采样条件下仍无收益。
若无正证据，不延长这一回报更新分支；即使探索目标受益，也不把打开部署噪声当成EMBER修复。
统一根因、可采纳改进及正确视频必要增量仍未验证。

Owner已指定本任务`01a0cd94-65da-7b22-8ca9-7ba35f454632`接管主讨论与科学判断，现有Sol任务
`01a0cd90-ebb7-77a1-a20b-a858825d2f66`负责具体实现和实验。双方实际身份已核对并登记，旧暂停不阻断既定四臂。
本批采用Queue派发；Sol完成或遇科学阻塞时主动回报本任务，完成后停止新增实验；主讨论核对原件、分析后再派发下一批。
不使用已停止Luna/旧主讨论ID，不新建任务或更改对方模型。四臂已完成，主讨论独立复核13200原始行，
继而完成1600个[冻结前段轨迹与视频条件干预](docs/designs/frozen_prefix_causality_design.md)，独立复核见findings§136；
继而完成1000个[接近阶段动作通道干预](docs/designs/approach_channel_causality_design.md)，独立复核见findings§137；
15900次[交叉视频函数诊断](docs/designs/crossed_video_action_field_design.md)也已完成，见findings§138；
随后[动作读出与内部适配因果分解](docs/designs/readout_realization_causality_design.md)已完成，见findings§139。
[任务关系支持](docs/designs/relational_support_causality_design.md)六臂固定1260及1500闭环已完成、独立复核，见findings§143。
补齐关系的C11未产生对C00的绝对收益；不能把正交互点值或语言B下降称为视频修复。
支持操作多数已有较好表现，同时C00→C10的Goal下降与早期接近偏移、干扰物先动相伴。
[原生读取Meta与其余Writer的冻结交叉](docs/designs/native_reader_transfer_causality_design.md)400条亦已完成、独立复核，见findings§144。
Goal四格39/38/19/28支持后续条件映射传递损害，读取适配部分补偿；不是统一根因或修复，也不是某个输出头的单独归因。
[单个任务当前信用的有限学习干预](docs/designs/support_slot_credit_causality_design.md)已完成、独立复核，见findings§145。
P/KEEP/SWAP/DROP的Object46/41/43/39、Goal40/42/45/48；新增76的第一步作用在五个固定Goal初态均远离plate，
保留77也未提供预期Goal保护。不能给task贴固定有害标签，不能把DROP的单任务高点称为修复，也不延长此窗口追逐预期。

[任务分组lookahead计算诊断](docs/designs/metatask_lookahead_credit_design.md)七组已完成并关闭，见findings§146。
TASK没有优于BASE；MIX变差不能当作TASK有效。普通FM与实际动作代理仍不等同于闭环能力，
正常BF16余项保留为辨识限制，不开启追逐精度的实验。

上一批[成功信用方向干预](docs/designs/return_credit_direction_design.md)已关闭，原机器合同
`configs/return_credit_direction_v1/experiment_spec.json`。八个合法训练task固定父C00@1155，先采集128条当前policy探索轨迹，
仅用官方成功标签形成一个共享回报梯度；与其反向、同teacher原FM方向各做一次相同参数步长的独立更新。
四格含未更新父，在独立初态及两条正确视频最多256闭环；零奖励差异时登记未识别而不虚构学习信号。
数学问题是闭环信用是否能经现有条件映射变成可转移的能力，方向反转提供对照；不是预设RL能救场。
本批FM与回报的query状态分布不同，步长也不保证相同功能变化；必须如实限制归因。
不预授权长RL、更多seed/尺度、held/官方Val/Test或架构改造，正结果也不直接证明视频必要性。
实际派发、资源与执行状态见[progress](progress.md)。

目标是提高正确条件下的绝对闭环能力，并建立有益的视频增量，理解能力保持和跨任务迁移；不是只做完一个矩阵。
固定LoRA是可达下界参照，不是公共底座课程。推理深度与方法边界以[Owner要求](docs/current_owner_requirements.md)为准。

Owner进一步给予持续优化和分析实验的长期授权；主讨论可在项目科学与资源边界内按证据自主安排后继工作，
不以完成四臂为整个研究的终点，也不为每一批重新索要同类许可。当前四臂数据、训练和选择合同保持冻结。
Owner随后明确：这次授权无需转发Sol；Sol专注执行已派发实验，机制分析、证据积累和下一批设计由主讨论负责。
Owner纠正主讨论在上一批结束、下一合同未定稿时停止的错误；主讨论继续分析到可执行并派发，不把执行者的批次停止点当自己的停止点。

## 与当前实验并行的方法研究

Owner要求开放架构与训练方式，参考元学习/VLA，同时保留两个月形成的自身特色。
主讨论已核对元模仿学习、Vid2Robot/VIMA、LAPA及Doc-to-LoRA原始来源，并与已有动态读取、原生纠正、
语义路径和功能蒸馏的正负证据对应，见[外部机制与EMBER主线](docs/analyses/external_mechanisms_and_ember_identity_20260924.md)。
这些文献没有直接选定新架构。已结束lookahead借鉴MLDG/Fish，但未取得超过BASE的收益，不恢复该路线的长训练。
成功信用正反方向干预未兑现收益；旧RL profile也只有工程依据，不启动长程RL。
Gaussian score经执行等价类条件化已验证完整梯度作用，但96条闭环没有兑现父能力保持；
现在用冻结探索开关区分目标差异，不追加学习算子。
继续区分条件信息是否可辨识、视频知识是否被提取、控制作用是否能编译、以及获取后的能力是否保持。
保留的特色是可检验的教学编译与原生知识利用；读取器、表示、参数生成和训练组织均可据证据实质重构。

## 每轮必须积累的科学证据

沿“教学内容→条件表示→生成LoRA→真实flow执行前缀→闭环状态演化”定位最早失效接口。
当前三个竞争机制见下文；它们不是待轮流尝试的改进清单。

评测先满足当前决策所需的证据，再按未解决的歧义有界追加；不预先为所有可能问题铺满矩阵。
每项补评明确其能改变的判断、独立信息来源、追加量和停止条件。现有关键checkpoint保留，允许有必要时再补，
但不自动补回取消节点，也不把一批预算变成永久禁令；主讨论按既有授权裁决并向Sol派发具体合同。

1. **实验前提出可区分的问题。** 写清数学/计算机制、必要假设、相关历史正负证据，以及至少一个会作出不同预测的竞争解释。
   指定主要因果变量、预期中间功能变化与闭环变化、否证条件；单纯换模块或排名不能代替机制检验。
2. **先确认干预测到了什么。** 用与主张相称的直接证据检查干预确实生效，并明确未隔离的变量。
   若干预未生效或精度不足以区分预测，记录为未识别，不把不显著结果硬解释为无作用。
3. **按事前预测裁决。** 同时核对机制读出、正确闭环、同任务换视频及能力获得/丢失。
   适用时用选择性修复或撤回所改机制的对照验证因果方向；不靠堆叠改动后的一次涨分宣布找到原因。
4. **把每轮结果接回历史。** 在findings记录证据编号、原件/commit、支持与反驳范围、剩余竞争解释和下一步理由，
   在research_history保留可追溯入口。正、负、未识别三类结果均保留；遇到反例修订理论，不遗忘或事后移动成功标准。

理论推导、机制实证和最终能力改善分别成立后，才把对应范围称为“原因已验证、修复有效”。
若视频没有收益，先区分数据提供的条件信息、表征提取、参数编译及训练辨识；若视频已携带有用内容但执行差，
再检验生成参数的功能误差、真实去噪过程及闭环状态偏移。实际选择由本批证据决定，不自动把每种可能都开成实验。
正常等待Sol完成期间不轮询运行日志/checkpoint/cache；故障迹象、即时调度或Owner明确询问时才做必要的轻量查询。
收到完整结果后集中核对原件，再派发下一批。

## 接管后的竞争解释与可反驳预测

以下保留四臂前的待测机制与预测；四臂已按原规则结束，其结果裁决见下文与findings§135。
已完成各批的实际裁决见各阶段及findings§144；这里的解释分支仍不是自动实验清单。
本次依据包括历史审计E01–46、9月11日专家完整论证、9月19日提案及撤回P-only的最终修订、近期§127–133、
真实实现与§134原始行复算。旧最终视频controls只说明各自冻结模型，不用于调四臂参数；Test不用于形成修复。

1. **共享生成更新的功能耦合，而非仅仅出口维数。** 令`theta_i=G_phi(l_i,V_i)`，`J_i=dtheta_i/dphi`，
   `R_i=d F(theta_i,x_i,xi)/dtheta_i`、`g_j=dL_j/dtheta_j`，其中F为真实10-flow后执行前5动作。普通SGD的一阶关系为
   `delta F_i ≈ -eta R_i J_i sum_j alpha_j J_j^T g_j`；AdamW须用实际完整状态的`delta phi`检验`R_i J_i delta phi`，
   不能把梯度余弦或某head范数代作伤害。能表示常量共享LoRA只说明可达解存在，不规定J建立的学习方向或有限优化轨迹。
   历史clone/shared、某些真实有限更新有跨条件损害；但当前private Writer16/32、freeAB14/32、H54缩头无收益、任务隔离无修复，
   反对直接归罪head容量或笼统任务冲突。预测：若本批A持续优于B且seen也分化，生成学习机制更值得查；若仅held分化，
   不能称已有任务学不会。若B/C在seen获取及Source保持均不逊A，则“生成链普遍妨碍学习”在本窗口被削弱。
   后继只有在固定原成功/原失败条件中复现**有害有限更新**，才考虑保留其它模块/条件时的功能干预；须同时保住新增能力，
   不能再由大head位移直接重跑H54式缩放，也不能把更新整体变小当作保持修复。

2. **教学内容的监督辨识与任务关系支持不足。** 理想独立跨episode采样为`p(T)p(V|T)p(X,A|T)`；
   若训练语言唯一指认T，则允许`p(A|X,l,V)=p(A|X,l)`。这说明目标容许忽略视频的最优训练预测器，
   不证明当前模型走了该解，也不证明视频不能改善有限数据或未见任务泛化。有限池排除teacher并非严格独立。
   当前Core的Value来自图文task-span与patch，**没有裸text residual**；但图文条件仍含目标语义，不能把所有Value来自视频等同于动态必要。
   同视频辅助可增加对所看轨迹的统计依赖，也可能把episode特有路径写进task LoRA：旧窗口主组1200比配对消融高38，
   后续2100变成−1；coverage里cross-episode辅助六节点均高于same-video。这些相反事实不支持固定宣布同视频配对有效/有害。
   预测：B学会seen而C/D在held没有correct及other的独立收益，与支持不足相容；若C/D在未见任务持续超过B且两种正确视频均受益，
   则“当前支持完全不能学出有益视频映射”的强版本被反驳。B/C活动模块不同，排名不能独立确定信息原因。
   后继数据干预应固定diagnostic-held8、Source/模型/有效更新及标签预算，只改变经BDDL审计的fit任务关系支持；
   “相关操作/对象组合替换”与“同等数量无关任务替换”给出不同预测，不用增加同task episode冒充增加独立映射。
   四臂数据固定，本轮不能确诊该解释；原meta73和12条件fresh反例要求拒绝“任务越多必然越好”。

3. **监督强调的速度场位置与真正执行函数不一致。** 实现为`x_tau=(1-tau)a+tau epsilon`、目标`epsilon-a`。
   tau1项等于`||epsilon-v(epsilon,1,c)-a||²`；理想平方风险下`epsilon-v`预测条件动作均值，并不保证视频被使用。
   普通FM的理想最优场连续延拓到tau1时也有相同条件均值；D改变的是有限模型中的监督分配，不能宣称更换了一个必然更正确的Bayes目标。
   mean50→mean5将第二组每个前缀位置名义系数增大10倍，真实梯度并非10倍。实际输出却来自十次迭代：
   `x_(k+1)=x_k-h v_theta(x_k,t_k,c)`，一阶误差满足
   `delta x_(k+1)=(I-h d_x v_k)delta x_k-h d_theta v_k delta theta`，所以第一端点MSE没有单独约束完整执行前缀。
   还有专家观测分布与policy自身到达状态的区别；主FM仍监督全50，动作去噪器的horizon交互使第二项未直接监督的后45位置也可能影响前5。
   预测：若D优于C，应落实到correct/other真实闭环、保持和固定行为阶段；若仅tau1或演示probe变好而闭环不改善，
   则“端点/前缀偏重足以解决当前问题”不成立。现有J/F54阴性仅限制短续训窗口，不代替fresh比较。
   若出现有益D效应，下一批才分别固定horizon改tau、固定tau改horizon；若出现演示动作好而执行差，优先用预先固定训练case的
   **同一保存动作前缀重放到相同环境状态后分叉**，区分当场动作选择错误与此前累积偏离；不把loss下降直接命名为occupancy根因。

三种解释可以并存。裁决先看同节点完整曲线与能力获得/丢失，再看固定selected的controls和post-freeze probe；
不能由一个最佳分数选择唯一机制，也不能为了符合预测修改既定选点。四臂完成后由主讨论核对原件，选择信息量最高的单项干预。

## 第一阶段：接住已有证据和未完成实现（已完成）

1. 先读Owner要求、progress、[concept](docs/concept.md)、findings§127–133，再沿[研究历史](docs/research_history.md)
   阅读与当前假设相关的完整旧论证、源码和原始结果。把事实、机制假设、已被反例削弱的解释分开，不能从零重新猜测。
2. 建立少量竞争解释：每个说明数学/计算机制、它解释哪些历史现象、预测哪些可观察差异、什么结果会推翻它。
   生成Jacobian、端点/前缀监督、训练任务支持只是已有候选框架；不得写成已确定的根因，也不要求机械各开一条新实验。
3. 从最新main隔离开发，逐项审阅并迁移`codex/conditional-compilation-diagnostics@73267f53`的WIP。
   先修复已知`SupervisedEngine.validate`缩进错误，再核验实际forward/backward、保存恢复和物化/评测接口。
   不能直接把旧分支整体覆盖main，不能因syntax compile通过就宣布工程实现完成。

上述历史复核和工程交接已完成；原WIP经修复验证后以`43d801b1`集成，核验边界见progress。

## 第二阶段：既定四臂定位（已完成）

接续设计为[条件编译四臂诊断](docs/designs/conditional_compilation_diagnostics_design.md)，机器规格为
`configs/conditional_compilation_diagnostics_v1/experiment_spec.json`；已授权接手后完成实现、核验及有界诊断，
工程合同通过后无需为同一四臂重复索要启动许可。本批全部训练、54个面板、selected controls及动作probe已完成，原始行复核通过。

| 比较 | 本批能够回答 | 不能直接断言 |
| --- | --- | --- |
| A直接共享rank16 → B语言Writer | 匹配划分/查询/时钟下，直接参数优化与条件生成链是否出现能力差异 | 单一头、rank或某个模块就是根因；A代表最优MT-BC16 |
| B语言Writer → C视频纯FM | 加入完整视频生成链后，获取、迁移和保持怎样变化 | 参数量完全匹配的纯视频信息因果效应 |
| C视频纯FM → D视频辅助目标 | 第二组tau1/前5步目标组合是否改变闭环与视频增量 | 单独是tau或horizon起作用；辅助项必然有益 |

fit28与diagnostic-held8都来自父协议Train36，官方24/8/8不改；旧MT-BC训练过这8个诊断留出任务，不能替代A。
每臂fresh1260更新、每步4×(21+7)查询、六节点210/420/630/840/1050/1260；节点比较held400及seen64。
先冻结选点，再对C/D做other/wrong；全部训练和选点结束后才做登记的无梯度动作探针。合计13200条登记闭环。
完整loss权重、RNG、数据墙、轨迹保存、资源及恢复要求以设计/spec为准，执行任务不能自行删臂、变配方或追加实验。
Source/MT-BC官方Test补测已完成；不重复补测，也不根据其分数改变划分、训练或选点。

## 第三阶段：冻结前段轨迹与视频条件干预（已完成）

四臂后的假设更新：B后四节点高于A，削弱生成链普遍受阻的强解释；C取得能力但Source保持更差；
D的正确视频相对wrong有优势，尚无超过B的绝对视频增量，末段held仍回退。C的held演示动作MSE更低却闭环更弱。
固定case出现错误对象，也存在相同任务成功的反例；9月14日完整行为回放和旧空间信用实验不允许直接宣布统一grounding问题。

选择一个能区分机制的下一步：冻结B630/C420，在global tasks14/21各50初态上重放B或C的前25/50动作，
再分别由B、C-correct、C-other、C-wrong接手。固定历史比较接手函数/视频条件，固定接手函数比较历史。
复用已有1600分支所需全部策略和banks，不训练、不选择新checkpoint、不再读取held expert actions。
本批已完成，完整结果见findings§136。Goal的B前段对不同接手者均更有利，Object无此普遍方向；
相同第25步物体body位置下机器人接近状态不同，支持早期状态的局部因果作用，尚未识别其具体运动分量或生成根因。

## 第四阶段：接近阶段动作通道干预（已完成）

按[设计](docs/designs/approach_channel_causality_design.md)固定原B630/C420、两个任务及50初态，
只改变前25步x/y与z命令的B/C来源，C的旋转/夹爪保持；加完整B前段，共五种prefix，两种冻结接手者，1000分支。
两个纯前段对照也在新合同运行，避免把新旧执行差异算作收益；不训练或改变视频条件。

区分水平接近偏置、下降/水平对齐时序、二者交互，以及其它通道/后续控制。
先核对实际命令与几何/接触改变，再按预定义主效应、交互和闭环裁决。干预无效或统计不充分可得到未识别结论。
即使行为恢复，也只是找到作用环节；仍须沿条件表示→LoRA→动作函数追查产生该偏置的学习原因，才能提出有依据的模型修复。

本批完整结果支持Goal的前段水平命令作用：C接手只换xy成功27→40，目标位移37→49、干扰物位移16→1；
只换z确实抬高末端却使成功27→19，联合xyz未高于xy。Object有不同方向及正确目标位移后的失败。
关闭本批，不扩动作通道扫描，不据此直接修改训练；假设及不确定性见findings§137。

## 第五阶段：同状态交叉视频动作函数诊断（已完成）

按[设计](docs/designs/crossed_video_action_field_design.md)复用冻结C420的每task50个正确视频banks，
在两个task各50初态、原C前段0/10/20步的相同query上全交叉，并加同query的B/Source/封存wrong参照，共15900实际10-flow预测。
不读取新expert标签，不执行本批预测动作，不训练、不选择新checkpoint或增加部署分数。

将C相对B的水平输出差异分成正确视频池共有分量与换视频的变化，再分视频主效应/状态交互。
这直接区分具体视频变化与池共有映射，不能把均值叫语言分量或把小方差判为视频无用；B也不是expert动作目标。
结果与已验证的命令干预共同约束后继表示、生成与学习机制假设，不从某个离线比值直接选架构。

已确认本窗口C/B函数差异主要为各正确视频共有分量，限幅不是主要解释；wrong仍产生函数作用。
Source的Goal保持损害和Object获得并存，不统一回退Source。历史已回答的Core/P交换、末P边界和task隔离不原样重做。

## 第六阶段：动作读出与内部适配因果分解（已完成）

按[设计](docs/designs/readout_realization_causality_design.md)固定C420的action_out与其余37个LoRA，作四格开/关。
复用300真实query作1200实际flow预测，记录最终读出直接作用及后续flow反馈；同时完成两任务四格的400配对闭环。
既检验Goal的旧能力损害，也保留Object的新能力反例；不按函数距离代替成功，也不把确定性冻结mask视作训练修复。
本批检验“参数作用通过哪个计算接口实现”，后继还须根据结果检验其学习来源；不默认冻结输出层、改优化器或增加新loss。

四格Object成功0/0/41/46、Goal35/33/30/28。内部37个适配保留了大部分Object新能力，也保留了主要早期函数变化；
移除action_out没有兑现Goal向B动作方向靠近的事前预测，Goal净+2的区间跨零且Object净−5。
关闭本批，不采用输出层冻结，不把内部函数贡献较大说成某个内部层已被定罪，完整边界见findings§139。

## 第七阶段：任务关系支持的学习干预（第一阶段已完成并关闭）

停止逐层/逐通道开关，直接改变形成适配的共享监督关系。以`c(o,d)=u_o+v_d`为明确的待检验低阶模型：
断开的对象—目标监督图存在分量间不受约束的相对偏移；补齐路径后，黑碗→炉面目标行可由已见关系的交替和表示。
这是生成干预预测的模型，不是实际LoRA相加或已证明的网络内部机制；Source/语言/场景和反向操作知识均可能突破它。

四个任务池共同26，i因子替换白碗“盘右region/盘上”，j因子替换平底锅“双层架内部/顶面”；
共同加入平底锅→炉面、白碗→同一架顶面。只有两因子都补齐的S11连通到黑碗→炉面。
每池仍28任务、每臂1260更新、141120动作query；四个fresh C配S00/S01/S10/S11，两个fresh B配S00/S11。
两组替换对完整BDDL仅语言/目标不同，场景/regions/初始定义一致；不把不同任务的专家轨迹冒称逐action配对。
同pool B/C全部事件一致，四池共同26的条件与query/RNG流一致。数据名单和审计见findings§140及新spec。

主预测是同1260节点Goal21的C正交互`p11−p10−p01+p00`与C11绝对改善，other也须兑现。
区分i/j一般主效应、两模型共同数据收益、支持操作未习得及非线性优化交互；不按哪个实验最高补故事。
第一阶段只做固定1260的两任务主比较、Goal21另一正确视频、新增支持操作与Source，共1500闭环/66cases。
保留原主问题50state与Object能力参照；支持先20state，阶段结论不足时再明确补测缺口，不先铺满9932矩阵。
1050相邻点、完整held400/seen64、wrong和3300函数预测暂缓；12个训练checkpoint保留，模型/训练行为不改。
上限data0 128GiB/data1后为保留E2与另冻E3修订到1.25GiB、同时6物理GPU；已完成臂与剩余训练重叠评测，Sol逐launch实时准入。
原协议/五批原件只读，无新官方Validation/Test、held expert标签、架构/loss/RL变化。

功能学习信用仍是竞争解释，未与支持不足混成同一干预。有限更新对实际执行函数的变化为
`F(G_(phi+delta_phi)(V,L),q)-F(G_phi(V,L),q)`；FM下降、梯度余弦或朝B靠近均不能代替能力获得/保持。
旧tau1、F54、任务隔离、H54及private/freeAB证据继续约束后继；主讨论可在Sol执行期间深化这一分支，
但不提前安排下一批训练、不用新loss名称重做已经失败的机制。收到完整原件后继续核验与决策，不等待Owner再次催促。

现已完成并独立复核，详见findings§143。C11没有对C00的Goal绝对收益，other还从40降至33；正交互点值的区间跨零。
新增支持大多较好，Source亦已有较高能力；没有验证图连通修复，也没有证明全部旧支持边已掌握。
保留C00→C10的具体有害数据敏感性及其行为线索，不自动补全deferred矩阵或继续训练。

## 第八阶段：数据效应的原生读取与其余Writer冻结交叉（已完成）

固定C_S00/C_S10@1260，N只含Text/VL/Action三组Meta，W含其余全部可学习Writer；三组Meta作用在同一冻结source原生坐标。
四格N0W0/N1W0/N0W1/N1W1分别生成一套完整LoRA，以两个任务各50状态、共400闭环检验N、W与共同适应的竞争预测。
读取端传递损害要求两个W背景中的N效应都符合方向，撤回N须恢复真实Goal能力，并查看Object代价；仅动作距离变化不够。
W主导或强交互分别限制后继设计，不按混合最高分挑部署模型。不会因这个诊断直接冻结Meta、改rank/loss或追加训练。
已有旧模块混合反例继续约束解释；本批新增信息是受控数据替换如何经固定原生读取边界传播，非一般模块扫描。
first5真实动作的有限两组分解、完整闭环与连续轨迹一起裁决，无额外函数预测、新expert标签、官方Val/Test或梯度。
规模/验收/资源/停止在[设计](docs/designs/native_reader_transfer_causality_design.md)，实际派发和实现状态见progress。

### 保留的后继解释分支

阅读完整配对曲线、逐task/suite、Source相对retained/gained/lost、跨节点保持及视频成功集合，而非只比较最佳总分。
以下是解释分支，不是预先批准给执行任务的追加实验清单：

- 若直接A明显强于B且差异也出现在seen任务，优先查条件生成的学习/参数化机制；若仅held分化，优先查条件映射的迁移。
  同一优化器和预算不等于各参数化都充分优化，必须保留竞争解释。
- 若B强于C，区分视频输入是否带来无关扰动、所增活动模块是否妨碍学习、或训练支持不足；不能直接判“视频没信息”。
  若C更强，要看correct和other是否都增加真实能力，不能只靠wrong下降判定有效。
- 若D强于C，下一步区分端点与执行前缀的作用，并定位相应行为变化；若差异不明，不能回头把旧辅助收益说成不存在。
- 若各臂seen能力足够而held共同不足，固定诊断目标来检验训练支持/组合迁移；四臂本身没有改变数据，不能据此确诊数据根因。
- 若所有臂都弱，先判断共同数据/执行合同是否有真实缺陷，以及固定优化窗口的局限；不能宣布EMBER或rank16到上限。

新主讨论结合已有反例选择信息量最高的针对性干预，先登记预测和否证条件，再检验具体机制能否同时改善正确闭环与有益视频利用。
执行任务完成本批就回报，由主讨论决定后继；不要无依据重复rank/LR/seed小扫、已完成消融或用不断续训替代解释。
较大修正训练应有机制依据和相应实证；小试训练侧涨分仍须独立检验fresh/未见任务迁移。当前尚未证明任何统一修复。

## 派发合同与交接边界

每次给执行session明确：假设与任务名、精确代码/spec、允许数据与输入、需改接口、固定变量、节点/选点/controls、
资源和输出位置、验收方法、异常处理、何时停止及向谁回报。科学歧义回主讨论，常规工程问题自行解决；不把科学设计交给执行者猜。
复用`codex-session-messaging`技能核对实际接收方；跨session消息加`由agent发送：`和来源ID。
修正当前工作用Steer，后续工作用Queue；出结果或有实际困难才回报，不发例行心跳。不要把入队回执当作已执行。

本批不自动扩展到官方Validation/Test、旧v5.2重训、FT/RL或新的修正训练；这些需由主讨论按新证据形成对应科学合同。
昨夜deadline、无上限GPU等特例不恢复，常规资源限制仍按AGENTS。清理后的旧权重可用性看retirement记录，勿假定所有历史checkpoint仍能原样加载。
仓库收尾已完成，无需重做大清理。最终向Owner直接解释发现、证据、剩余不确定性及解决方案，不把报告文件当作回答。


## 第九阶段：从同一学习状态拆开新增与删除（当前）

第八阶段已经完成；Goal的W效应在两个N背景均有害，N1在W1下反而补偿，初始函数与plate运动方向相符。
这是作用路径，仍未回答为什么学成这样。原77→76干预同时删除和新增，不能据此把76直接称为坏数据。

三臂从C00@1155完整状态，共同1156..1183的108个公共event固定，4个slot分别保留77、换76、屏蔽77当前信用。
不清历史动量、不删其它梯度、不重归一化；整个Writer仍联合学习。有限函数作用A=swap−drop、R=keep−drop，替换作用=A−R。
若swap相对drop先向plate偏、再损害Goal，支持这段新增信用的有害传递；若keep相对drop保护而swap接近drop，支持保护丢失。
若共同变化占主导或只有内部差异，不能称已证实该学习根因；短窗阴性亦不否定1260全程路径依赖。

固定1183闭环两任务×50×四模型=400，未更新父1155捕捉共同演化；首个干预1160后30次真实flow用于先于轨迹分叉的预测。
合法供体76/77的128条无梯度FM仅检查监督响应，不能代替成功。单seed、四次slot曝光、局部保留范围均明示。
具体数据、原始事件索引、统计、验收、资源与停止在新设计/spec；旧§44–46、§127、§129等反例随合同保留。
完成后根据对新增/保护/路径解释的实际区分，再决定功能保持或结构改法，不由模块名、梯度大小或排名直接开fresh。
