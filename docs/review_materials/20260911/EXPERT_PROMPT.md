# 请审查EMBER现有方法，并建议下一阶段的实质改进

你是一位熟悉机器人模仿学习、视觉语言动作模型、视频到策略迁移、超网络和多任务优化的独立研究专家。请基于远程仓库的实际代码、历史和已提交实验记录，审视EMBER目前的失败，提出改进现有方法和继续推进的建议。请独立判断，允许推翻我们的解释；不要只复述指标或给泛泛的调参清单。

仓库：https://github.com/LinFyM/EMBER 。请以本prompt所属提交为审阅快照。先读同目录`README.md`、`index.json`和`panel_summary.json`，再读仓库根目录`progress.md`、`task_plan.md`、`findings.md`相关§39–49、`docs/current_owner_requirements.md`、`docs/concept.md`和当前设计。历史路线按`docs/research_history.md`索引追查；9月7日证据包和9月8日专家原文保留了重要反证和Owner裁决。不要一次把所有JSON塞进上下文。

你只能看到远程仓库，因此本次已提交119个评测面板、13,901条逐条结果、8个学习run的原配置/commit及训练曝光、640个视频条件的内部数值、有限更新和端点诊断、行为图片以及干预脚本快照。用本目录`verify_evidence.py`可直接复算主要统计。原始`runs/`、模型和数据未上传；index的source只表示来源，应读exports。不能假定能够访问本地文件，缺证据时请具体指出缺口。

特别注意：**main不是126分候选。** main仍启用Compiler额外语言query；最强候选来自`45e16633394e1baa9af8cefba698661b41aab539`的`backend_conditioning=local_h_read`、init7 macro400，只关闭额外Compiler route，保留exact language/local/H-read与独立D。独立D仍跨task共享。main新接通dual输入，所有有成绩的checkpoint实际均为agentview单视角，不能把输入smoke当成双视角训练。

科学目标是在rollout前，用exact task language和action-hidden正确视频一次生成冻结source的唯一完整38-target LoRA，从未见初始化闭环执行。严格目标为validation8 single-checkpoint correct>145/400，并满足相邻/换视频稳定、低churn、breadth、四suite/Goal/Long和最终因果要求。禁止deployment读取teacher动作/state/reward/task ID，禁止task-local优化，validation/test无梯度；Test仍封存。当前K=1、pure FM、Writer与观察Meta fresh端到端训练，source冻结；视频和动作query同task跨episode。具体rank、memory token、Compiler/decoder形式可以讨论，不是目标本身。额外non-held真实meta tasks需严格审计排除held及重复项，不制造人工process数据/新任务绕过问题。

当前核心事实：

- all init7验证103→90，Compiler-off108→126；init11 all100→108、off104→119。删除额外route有两个初始化的净收益，但off init11仍丢37个验证旧成功，BBQ35→19。两个off仍在增长，尚不能认定平台。
- 同target跨rank D绑定训练32→60/96，验证115→82/400。它有训练学习，却损害迁移和保持；不能外推所有共享均有害。
- 最强126候选的九臂固定train24×四初态检查：完整正确53、另一正确60、同suite错误59、跨suite错误59、乱序55、倒序52、首/中/末静态58/53/53，均/96。正确动态视频仍未形成稳定整体优势；输入也确实改变具体成功集合，不能直接称完全忽略视频或纯task记忆。
- 查询差异压缩有真实读取证据，但强制拉开双层查询差异并无一致即时闭环修复。all背景四次其它task真实更新可使橙汁转选BBQ，零当前梯度Adam对照仍成功；P变化可复现。BBQ四实例只换D400已有3例转选绿色瓶，P400有2例，C400为0，且有交互。它们是局部因果证据，不是全局率，也不是最强off模型的同类更新实验。
- 历史v5.2/v6纯FM及视频→LoRA已有较强闭环，v6曾143；GOMQ151单点未保持且有rank合同边界。source47、SFT109/107等参照与不同架构优点不能拼成一个模型。请用历史约束解释，避免把旧路线改名重做。

我们的暂定看法是：问题涉及正确视频证据的可辨识性、条件表示到策略参数的映射和跨任务获取/保持，不能仅归Compiler query几何。固定task的language/static线索可能满足FM，但这尚不证明模型纯记task；也不能推出必须换RL。prior加视频残差、函数保持约束、合法meta-task扩展等只是未验证设想，不是已支持的解决方案。

请回答：

1. 按证据强弱，最主要的瓶颈是什么？我们的归因有哪些错误、遗漏或替代解释？请区分架构事实、行为干预支持、数学推导与未验证猜测，并指出最早失效接口。
2. 这些结果具体支持哪些修正？请优先给出少量最值得做的方案，说明改动模块/数据流/损失或训练组织、保留部分、因果机制、历史近等价尝试、预期行为变化和可能代价。允许实质创新，但不能只因现有代码已经写了就围绕它加补丁。
3. 如何让正确视频中的目标关系和动态操作过程贡献可迁移增量，同时避免共享更新破坏其它条件的目标选择？哪些建议有当前证据支持，哪些必须先验证？若现有真实数据无法区分所需能力，请明确说明为何以及最小合法补充是什么。
4. 下一步最少、最有区分力的实验应该是什么？优先使用当前最强候选或说明为何必须用别的模型，给出主要因果变量、配对对照、预算/曝光、真实闭环节点，以及不同结果对应的继续/放弃决定。不要把又跑一轮400步当作默认前提，也不要为看懂一个现象无止境加分析。
5. 如何组织后续完整推进：先解决什么、何时继续学习、何时做架构修改、何时考虑合法数据扩展或独立共享RL，以及怎样同时裁决绝对能力、保持、breadth与视频必要性？双视角真实学习、Compiler-off续段及每100步strict400已是已知待办，请评价其位置，但不要把它们当作本次分析带来的全部修正答案。

请给出明确的优先建议和技术理由，引用具体文件/表/代码。必要时使用简洁数学或完整数据流解释。不要承诺未经验证的改动必然有效，也不要以泛泛的“更多数据、更大模型、调学习率、继续训练”收尾。当前只咨询，实验与正式采纳由Owner随后决定。
