# 第二轮追问：普通监督曾学出视频依赖，当前方法为何没有复现？

感谢上一轮审查。我们认可你对证据边界、查询几何局部性、获取与保持区别的分析，但希望你重新审视核心问题和推进优先级。请把本次作为对[上一轮回复原文](expert_review_round1.md)的补充与修订，明确哪些原建议保留、降级或替换，不必重写整份现状报告。

仓库为 https://github.com/LinFyM/EMBER ，本次讨论以本文件所属提交为准。科学证据包仍是[README](README.md)、[index](index.json)和[panel_summary](panel_summary.json)所列的119面板、13,901条结果；本次仅补充讨论，没有新训练或评测。main仍不是126候选；其身份为`45e16633394e1baa9af8cefba698661b41aab539 / local_h_read / init7 / macro400`，单视角、独立D。你只能读取远程Git与已导出的记录，不应假设本地runs可访问。

## 1. 首先正面解释v5.2这项历史正证据

Owner特别强调：**v5.2用普通正样本动作监督，没有视频contrast/order或过程辅助loss，已经产生了明显的正确视频依赖。因此“不增加辅助损失也能学出视频依赖”已有实证，架构与学习路径必须进入核心解释。**

原始v5.2 step900（训练commit `529da6bbe290f7393422937aa7cc278cee732107`）固定validation8五臂为：

| 输入条件 | 成功数 /400 |
| --- | ---: |
| 正确视频 | 132 |
| 同task另一正确视频 | 138 |
| 跨suite错误视频 | 74 |
| 真实帧乱序 | 82 |
| 真实帧倒序 | 83 |

两条正确视频关联都明显高于这些历史控制的总数，不是只在某一条教师视频上出现的现象。原始材料：

- [v5.2设计](https://github.com/LinFyM/EMBER/blob/529da6bbe290f7393422937aa7cc278cee732107/docs/action_forecast_writer_v5_2_design.md)：§2明确normal-order positive-only动作监督，不允许contrast/order loss。
- [实际配置](https://github.com/LinFyM/EMBER/blob/529da6bbe290f7393422937aa7cc278cee732107/configs/pi05_as_writer_language_axial_v5_2.json)：`single_video_multi_action_positive_functional_loss`，以及读取、训练和曝光合同。
- [真实训练step](https://github.com/LinFyM/EMBER/blob/529da6bbe290f7393422937aa7cc278cee732107/src/ember/writer/as_step.py)与[functional接口](https://github.com/LinFyM/EMBER/blob/529da6bbe290f7393422937aa7cc278cee732107/src/ember/writer/functional.py)：主动作loss经生成LoRA回传Writer。
- [已导出的五臂合同、汇总和逐条结果](../20260907/records/historical_v52/)，其中采用`step0900_529da6b`记录；不要与task-complete版本混同。
- [历史账本§2](../../research_history.md#early-writers)与[9月7日评审](../20260907/expert_review_round1.md)：保留交叉recipe结果及后续解释边界。

我们不把这些分数升级为v5.2已通过全部科学资格，也不把旧400与当前train24诊断96当成同口径差值。错误/乱序输入的分布变化和缺少公平训练static baseline仍限制“普遍动态理解”的结论；历史controls只用于解释既有结果，不成为新架构、loss或checkpoint的搜索信号。但这些边界不能抹掉普通监督曾学出正确视频依赖的正证据。

请回答：你的“现有监督允许忽略V的解”同样适用于这类旧训练。**为什么旧方法能学出明显依赖，而当前最强候选没有建立正确过程的稳定净增量？** 如实际采样或监督合同有关键不同，请指出源码与真实run依据；否则这个分布推导不足以解释新旧差异，更不足以直接提高辅助loss相对于架构修正的优先级。

## 2. 把“存在捷径”与“为何学到捷径”分开

你给出的`p(T)p(V|T)p(O,A|T)`推导有价值。在语言充分识别训练task的假设下，它说明风险最优解可以不依赖视频；但没有说明有限任务、有限样本、特定参数化和优化下，训练必然收敛到这样的解。

EMBER要求视频帮助模型学会如何执行并迁移到新初始化/任务。语言能标识目标，不等于有限训练后的模型已经知道如何执行；我们没有要求人为制造含糊语言或新任务，使视频在信息论上成为唯一可能的信息来源。

请区分：训练task身份可辨识、视频语义贡献、视频动态过程贡献，以及未见task上的实际学习/迁移收益。你的下一轮建议究竟修复哪一层？若仍把监督结构列为当前首要原因，请解释它怎样与v5.2正证据相容，并给出超出“目标没有强制使用视频”的具体学习机制。

## 3. 请补上旧架构到当前架构的机制比较

我们不要求机械恢复v5.2，也不认为历史非匹配比较已经证明某个模块是唯一原因。v5.2 task-complete的五臂为120/109/107/111/124，说明架构名称本身也不是保证；训练组织与架构的交互不能省略。

但目前你的方案主要保留整张当前图，再改采样或增加loss，对架构在“普通监督最终学到什么”中的作用讨论不足。请沿真实信息流与反向学习链，比较下列有实质差异的接口，选出少量最值得怀疑的机制，避免逐项罗列猜测：

- v5.2的task-query patch grounding、图文Semantic Core与有向Procedure分别保留和消费什么；当前从原生H响应、局部关系与视觉核实到P4的路径是否更容易保留共同语义、丢掉与执行有关的变化？完整H和更复杂处理本身不能回答可学习性。
- 旧Core条件化读取经过时间中心化的Procedure，再通过slot融合进入FactorHeads；当前P4共同进入Compiler和独立D。比较的是动态信息到有效策略的消费方式、信用路径和条件耦合，不是模块名称或attention差异。旧源码见[temporal.py](https://github.com/LinFyM/EMBER/blob/529da6bbe290f7393422937aa7cc278cee732107/src/ember/writer/temporal.py)。
- v5.2配置有Text/VL/Action三组观察侧Meta；当前只有Action Expert侧Meta、vision/Gemma冻结。这是否限制了与目标对象/关系相关的视觉表征适配？请区分事实与猜测，不把它自动转成“把所有backbone解冻”。
- 旧FactorHeads与当前约3.30亿参数的D，其条件分辨、共享任务映射、输出空间和优化难度有何实质区别？参数量或代码相似度本身都不能证明根因。此前D绑定和查询干预也没有穷尽所有合理参数化。
- 核对教师池、独立条件曝光、动作曝光、更新次数与任务共现。旧step900配置为75,600动作queries、3,600次单视频条件；当前macro400为102,400动作queries、1,600次条件。更多动作query不能自动代表对视频→策略映射学习更充分，旧50-episode池与当前16条教学池的区别也须保留。

请给出一张简短对照：**已核实差异 → 候选学习机制 → 支持/反对证据 → 最有区分力的验证**。允许结论是架构与训练交互尚未识别，但不要因尚未唯一定位就默认架构无需实质修改。任何建议都须保持当前完整H、真实prefix和过去定向等Owner边界；若认为必须修改稳定边界，请明确提出理由供讨论，不静默恢复旧horizon mean等路径。

## 4. 为什么把保持实验设为视频问题的前置步骤？

当前最强候选在固定train24面板上，完整/另一正确/同suite错/跨suite错/首帧为53/60/59/59/58（各96）。视频改变了成功集合，却没有建立正确过程的稳定优势。功能保持与8-task共现可能改善绝对能力，但改善后仍可能主要依赖语言/静态线索。

你已经承认功能保持不能解决正确动态为何有用，实验B却要求先做实验A。两者是否有已经证实的依赖关系？若没有，为什么把最核心的科学缺口排后？保留off400作为对照与保留其全部架构作为唯一改进基座，也应分开。

另请解释功能保持的两个风险：成功锚点是否可能同时保护当前的语言/静态依赖；少量旧成功能否代表对未见条件有用的正确函数？all7局部更新损害支持“存在破坏”，没有证明它是off视频失效、Spatial迁移不足或Long获取不足的共同主因。请不要把同一个保持实验的多种可能收益统称为视频学习修复。

## 5. 真实动作过程辅助头，怎样影响最终策略而不止改善可读性？

你建议从`P4_u,P4_t,Δ`预测教师已发生的动作。即使部署删掉辅助头、监督位于实际P4，也仍可能只在Compiler不用的子空间读出动作。历史G2已提示“动态表示可用”与“共享策略生成成功”之间存在断点。

请说明这项方案相对于保持普通FM、修正视频消费路径的具体优势，并回答：

- 什么机制让辅助监督改善最终LoRA，而不只增加教师动作重建能力？若无法保证，哪种行为证据能分开“表示没学到”和“表示有了但生成端没用好”？
- 教师的低层动作混有其初始位姿、机器人运动和演示风格；这些与部署的新初始化不同。怎样使辅助任务偏向可迁移的对象关系和操作过程，而非奖励原轨迹细节？视觉不足以确定的动作成分及stride5对齐怎样处理？这也关系到action-hidden视频教学的科学动机。
- “普通新增动作监督、当前帧辅助头、过去—当前辅助头”具体如何对齐标签、主FM、条件曝光与计算？仅使用有记忆的P4单时刻不能称为纯当前帧基线。
- 过程辅助头改善闭环本身仍不能证明是动态信息起效。哪些预登记正数据验证值得投入，哪些结论只能留到候选冻结后的独立视频因果裁决？

我们允许辅助监督成为有依据的候选，但不接受“普通FM没强制使用视频，因此应先加辅助loss”作为充分论证。

## 6. 实验预算与参照需要围绕可裁决的问题设计

你提出A最多24步、B最多32步，并从off400继续。短程更新可以检验局部破坏和保持，却未必足以判断从已有语言/静态依赖中重新学出视频消费。请明确各实验能否定什么；负结果不能被扩大成架构或过程学习不可能。如果改动有结构性不兼容，应解释何时必须fresh，而不是用短续训检验其全部潜力。

learned language/static参照不必现在无条件重跑若干400步，但其定义应现在进入设计。若直到“值得正式确认的候选”出现才讨论，早期进展用什么证据区分一般策略变强与视频教学变强？请明确训练侧诊断、预算筛选和正式资格三者的边界，不用短训static落后作必要性证明，不反复用已看过的乱序/倒序差额选模型。

请按独立条件覆盖、动作曝光与真实算力成本说明最小有信息量预算，避免任意24/32步成为新的科学门槛；也不把多个短臂和密集闭环累加成默认大轮实验。此处是请你修订建议，不是授权执行新的训练。

## 7. 请据此给出修订后的优先结论

请先正面回应v5.2反证，再给出**一个主假设与一个最强竞争解释**，以及下一项最值得投入的实验；不必为上面每个问题各开一条训练线。

我们需要的是：从完整`语言+正确视频 → 操作证据 → 唯一LoRA → 自身观测下的执行`推导，说明怎样让普通监督更容易学出真正有用的视频依赖，或者为什么确有必要改变监督。请同时保留绝对成功率、弱任务获取和保持要求，避免通过压低static/wrong表现人为制造差额。最终目标仍为strict validation400>145和原有稳定、breadth、跨视频与因果资格。

请明确上一轮哪些判断应修正、哪些仍成立；每个核心归因引用具体代码或证据，区分已确认、合理推断、尚需验证。不要把v5.2仅写成“纯FM曾有能力”后继续绕过它，也不要把Owner指出架构的重要性理解为必须恢复某个旧模块。当前仍只咨询、没有采纳新设计或恢复科研执行。
