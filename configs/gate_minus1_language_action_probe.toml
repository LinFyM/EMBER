schema_version = 1
name = "gate_minus1_official_overlap_same_observation_language_action"
surface = "official_overlap_mechanics_only"
pilot_config_sha256 = "9ad5bbc421f8ae2d2701db8d39415d246280f6c1dcb46bc80546162cd7d5b913"
prior_pilot_result_sha256 = "e824673e143157319d17b50a044962d5c7487d9edbc2bddc983ed309818b4219"
conditions = ["correct", "no_spec", "scene_only", "swapped"]
repeat_condition = "correct"
primary_comparison = "correct_vs_swapped"
planned_action_steps = 10
environment_resets_per_task = 1
action_atol = 1e-6
action_rtol = 1e-6
repeat_atol = 1e-7
repeat_rtol = 1e-7
substantive_plan_max_abs_delta = 0.01
minimum_substantive_fraction = 0.80
known_cross_batch_max_abs_delta = 0.002254
substantive_threshold_rationale = "0.01 is over four times the previously measured batch-1-versus-2 maximum action delta; this probe keeps batch fixed at eight"

[mechanics_contract]
same_cached_reset_observation_across_conditions = true
same_fixed_batch_shape_across_conditions = true
same_policy_rng_seed_across_conditions = true
policy_reset_before_each_condition = true
correct_repeat_required = true
cross_batch_pooling = false
upstream_policy_forward_unchanged = true

[resource_contract]
gpu_count = 1
expected_peak_vram_mib_lt = 8000
coordinator_processes = 1
simulator_worker_processes = 8
maximum_total_processes = 9
expected_wall_clock_minutes_lt = 5
stop_conditions = ["prior_pilot_authority_mismatch", "reset_identity_failure", "correct_repeat_instability", "error"]
cleanup = "retain checksummed JSON and compact HTML; no new rollout videos because the linked prior pilot already retains matched videos"

[interpretation]
authorized_claim = "same-observation language-to-action causal-path diagnostic on an overlap-trained checkpoint"
gate_decision_authorized = false
forbidden_claims = [
  "correct same-init goal switching",
  "LIBERO-90 Gate -1 pass",
  "policy quality",
  "Writer utility",
  "video utility",
]
